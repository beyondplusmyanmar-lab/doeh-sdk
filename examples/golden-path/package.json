{
  "name": "doeh-golden-path",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for the canonical Orders example + a CI drift gate that fails if any surface (SDK quickstart, OpenAPI, portal) diverges from order.json.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
