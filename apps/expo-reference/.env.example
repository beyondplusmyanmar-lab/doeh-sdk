# Optional dev convenience: prefill the Settings screen with a sandbox key so you
# don't have to paste it on every reload. NEVER commit a real key.
# Leave unset and paste your key in-app (stored in the device secure store).
EXPO_PUBLIC_DOEH_API_KEY=
